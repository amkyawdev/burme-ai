# Cloudflare Workers/Pages Configuration

name = "burme-ai"
compatibility_date = "2024-04-16"

# Build settings for Cloudflare Pages (Python)
[build]
command = "pip install -r requirements.txt"
cwd = "."
